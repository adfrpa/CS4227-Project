// This code defines a Java interface named "Interceptor"
public interface Interceptor {

    // This line declares a method named "intercept" that takes a single parameter of type "ContextObject".
    void intercept(ContextObject context);
}
